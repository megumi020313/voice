# GitHub 上传说明

手动执行命令：

```bash
# 1. 进入项目目录
cd "e:\课题组\声纹识别项目\remote代码备份\vioce 1.20"

# 2. 删除锁定文件（如果存在）
# 在文件资源管理器中删除 .git\config.lock 文件

# 3. 初始化 Git 仓库（如果未初始化）
git init

# 4. 添加远程仓库
git remote add origin https://github.com/megumi020313/voice.git
# 如果已存在，使用：git remote set-url origin https://github.com/megumi020313/voice.git

# 5. 添加所有文件
git add .

# 6. 提交更改
git commit -m "Initial commit: 声纹识别项目代码"

# 7. 设置主分支
git branch -M main

# 8. 推送到 GitHub
git push -u origin main
```

## 身份验证

如果是第一次推送，GitHub 会要求身份验证。可以使用以下方式：

### 方式一：Personal Access Token（推荐）
1. 访问 https://github.com/settings/tokens
2. 生成新的 token（需要 `repo` 权限）
3. 推送时，用户名输入你的 GitHub 用户名，密码输入 token

### 方式二：GitHub CLI
```bash
# 安装 GitHub CLI 后
gh auth login
git push -u origin main
```

### 方式三：SSH 密钥
1. 生成 SSH 密钥：`ssh-keygen -t ed25519 -C "your_email@example.com"`
2. 将公钥添加到 GitHub：https://github.com/settings/keys
3. 使用 SSH URL：`git remote set-url origin git@github.com:megumi020313/voice.git`

## 手动上传 remote/remote-v2/data 目录

### 步骤说明

`remote/remote-v2/data` 目录已配置为：**除了音频文件，其他所有文件都可以上传**。

### 手动上传步骤

1. **进入项目目录**
   ```bash
   cd "e:\课题组\声纹识别项目\remote代码备份\vioce 1.20"
   ```

2. **清理 Git 锁定文件（如果存在）**
   ```bash
   # PowerShell
   Remove-Item .git\index.lock -Force -ErrorAction SilentlyContinue
   
   # 或手动在文件资源管理器中删除 .git\index.lock 文件
   ```

3. **添加 data 目录到暂存区**
   ```bash
   git add remote/remote-v2/data/
   ```

4. **检查将要提交的文件**
   ```bash
   git status remote/remote-v2/data/
   ```
   
   应该会看到以下文件被添加：
   - `remote/remote-v2/data/calibration/as-norm.py`
   - `remote/remote-v2/data/calibration/calibrate_snorm_threshold.py`
   - `remote/remote-v2/data/calibration/calibration_result.png`
   - `remote/remote-v2/data/calibration/compute_as_norm_score.py`
   - `remote/remote-v2/data/calibration/trials_list.txt`
   - `remote/remote-v2/data/prepare_cohort_targets.py`
   - `remote/remote-v2/data/raw_long_audio/slice_long_audio.py`
   
   **注意**：音频文件（.wav, .mp3 等）不会被添加，这是正常的。

5. **提交更改**
   ```bash
   git commit -m "添加 remote/remote-v2/data 目录（除音频文件外）"
   ```

6. **推送到 GitHub**
   ```bash
   git push origin main
   ```
   
   如果遇到身份验证问题，请参考上面的"身份验证"部分。

### 验证上传的文件

上传成功后，可以使用以下命令验证：

```bash
# 查看远程仓库中的文件
git ls-tree -r main --name-only | grep "remote/remote-v2/data"
```

或在 GitHub 网页上查看：https://github.com/megumi020313/voice/tree/main/remote/remote-v2/data

## 故障排除

### 问题：Git 锁定文件无法删除
**解决**：
- 关闭所有可能占用 Git 的进程（VS Code、Git GUI 等）
- 手动在文件资源管理器中删除 `.git\index.lock` 文件
- 如果仍然无法删除，重启电脑后重试

### 问题：data 目录中的文件没有被添加
**可能原因**：
1. 文件是音频文件（.wav, .mp3 等），会被自动忽略（这是正常的）
2. 文件已经被 Git 跟踪，没有更改
3. `.gitignore` 规则配置有误

**解决方法**：
```bash
# 检查文件是否被忽略
git check-ignore -v remote/remote-v2/data/calibration/as-norm.py

# 如果返回空（退出码 1），说明文件不会被忽略，可以正常添加
# 如果返回规则，说明文件被忽略，需要检查 .gitignore
```

### 问题：推送时要求身份验证
**解决**：
- 使用 Personal Access Token 代替密码（推荐）
- 配置 SSH 密钥
- 使用 GitHub CLI

### 问题：网络连接问题
**解决**：
- 检查网络连接
- 如果使用代理，配置 Git 代理：
  ```bash
  git config --global http.proxy http://proxy.example.com:8080
  ```

## 验证上传

上传成功后，访问 https://github.com/megumi020313/voice 查看仓库内容。

