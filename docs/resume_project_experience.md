# 简历项目经历（全量版，音频 / ASR / 无障碍语音辅助方向优先）

> **文档说明**：本文档基于仓库内 `SMP/README.md`、`remote/remote-v2/README.md`、`remote/brain_core/README.md`、`ASR精度提升技术方案.md`、`更换模型方案书.md`、`部署指南_Windows_macOS.md` 及工作总结文档整理而成，面向**音频 / ASR / 构音障碍无障碍辅助（羽言项目）**比赛场景进行关键词与排序优化。  
> 所有技术名词与仓库 README 保持一致；不含内网 IP、密钥、内部服务地址等敏感信息；量化指标仅引用仓库文档中有明确记录的数值。  
> **项目排序说明**：项目 1–3 为音频 / ASR 核心方向（比赛重点展示）；项目 4–6 为工程能力支撑（按岗位按需选用）。

> ⚠️ **使用前必读**：文档中所有标注为 `[填写]` 的字段均为占位符，**提交前请逐一替换为真实内容**，包括项目归属单位、个人角色、起止日期等。

---

## 一、完整版项目经历

### 项目 1：实时语音转写与流式字幕 / 翻译 / 总结系统（重点）

| 字段 | 内容 |
|------|------|
| **项目归属** | [填写：个人 / 实验室 / 公司名称] |
| **个人角色** | 后端算法工程师（在已有前端框架上扩展后端全链路） |
| **周期** | 2026.03 — [填写：持续 / 结束月份] |
| **技术栈** | Python · FastAPI · Flask · WebSocket · Faster-Whisper · Silero VAD · ffmpeg · Hunyuan-MT / Ollama（Qwen2.5） |

**项目简介**  
面向真实教学/沟通场景的端到端实时音频处理系统。系统将音频流解码、VAD 语音段切分、ASR 转写、机器翻译与 LLM 流式总结串联为完整链路，通过 WebSocket 向前端实时推送字幕与翻译结果，支持多会话管理与断线重连。在构音障碍辅助场景下，该链路可直接复用：将麦克风采集的发音模糊音频输入同一管线，结合热词与提示词模板大幅提升非标准发音的识别效果。

**工作要点**
- 设计并实现端到端实时音频处理链路：音频流经 ffmpeg 解码重采样 → Silero VAD 语音段检测与切分 → Faster-Whisper ASR 推理 → 翻译 / LLM 总结 → WebSocket 字幕流推送，支持会话状态管理与异常兜底
- 针对 VAD 切片策略进行工程化优化：调整 chunk size、重叠窗口（overlapping）与动态静音阈值，降低构音障碍者短促 / 模糊发音被漏检或截断的概率
- 后端集成领域专属提示词（prompt engineering）：通过自定义词表与上下文模板提升专有词汇（如辅助沟通场景中的高频用语）的识别召回率，可扩展为热词加权解码与个性化词表接口
- 探索流式体验优化方案：对比轻量模型（减少推理延迟）与重型模型（提升准确率）的权衡，记录 VAD 参数与模型选型对端到端延迟的影响，为后续产品迭代提供决策依据
- 架构预留扩展点：会话层支持声纹过滤（Speaker ID 接入）、噪声增益控制与降噪前处理模块的热插拔，适配嘈杂环境下的构音障碍沟通场景

---

### 项目 2：声纹识别与说话人区分系统（Remote-V2）

| 字段 | 内容 |
|------|------|
| **项目归属** | [填写：个人 / 实验室 / 公司名称] |
| **个人角色** | 算法工程师（模型集成、推理优化与服务化） |
| **周期** | 2025.12 — [填写结束月份] |
| **技术栈** | Python · PyTorch · ERes2NetV2 · Silero VAD · AS-Norm · FastAPI · Flask · CUDA |

**项目简介**  
基于 ERes2NetV2 声纹嵌入模型与 Silero VAD 的说话人验证与区分系统（V14.2）。系统在嘈杂环境下通过 VAD 优先策略（"VAD 为王"）提取有效语音段，再经声纹嵌入与 AS-Norm 自适应归一化评分判断说话人身份。适用于构音障碍辅助沟通中的**声纹过滤**（过滤周围杂音说话人）与**个人声纹注册**场景，是羽言产品中"声纹绑定 + 身份验证"能力的核心算法基础。

**工作要点**
- 构建完整声纹识别链路：Silero VAD 裁剪长静音段 → Kaldi fbank 特征提取（全局 CMVN 归一化） → ERes2NetV2 嵌入推理 → 余弦相似度 + AS-Norm 评分 → 说话人判决，可区分多人混说场景
- 实现批量推理优化（V14.0）：Mini-Batch 切分处理长音频，推理速度相比单段串行提升 **10–50 倍**（数据来源：仓库 README V14.0 版本说明），显存占用约 1.5–2 GB，可与 ASR 服务共卡部署
- 引入 AS-Norm（自适应分数归一化）：利用队列样本对原始余弦相似度进行统计校正，提升跨场景（不同收音设备、不同噪声底噪）的鲁棒性，降低误识与拒识率
- 服务化封装（FastAPI + Web 测试界面）：提供健康检查（Sanity Check）脚本，确保特征前端与模型推理前端完全一致；接口支持音频文件与实时音频流两种输入模式

---

### 项目 3：ASR 精度提升与模型迁移方案（噪声/方言/热词场景）

| 字段 | 内容 |
|------|------|
| **项目归属** | [填写：个人 / 实验室 / 公司名称] |
| **个人角色** | 算法研究与方案设计 |
| **周期** | [填写：起止月份] |
| **技术栈** | Python · Faster-Whisper · Silero VAD · FunASR（Seaco-Paraformer） · FSMN-VAD · DeepFilterNet / BSRNN · LLM RAG（Qwen2.5） |

**项目简介**  
针对嘈杂环境（高噪、多方言、多人混聊）下 ASR 识别率低的问题，系统研究并设计七类精度提升方案，覆盖"前端信号处理 → 中端模型优化 → 后端语义修正"全链路。同时完成从 Faster-Whisper 架构向 FunASR Seaco-Paraformer 的迁移方案设计，显式支持热词加权解码。该研究直接对应构音障碍人群的核心痛点：发音模糊、识别率低、嘈杂环境沟通难。

**工作要点**
- 系统梳理 7 类 ASR 精度提升方案（含方案对比矩阵），按实施难度与收益给出优先级路径：VAD 切片优化 + Prompt 工程为低成本必选；AI 降噪 / 方言路由 / LLM 纠错为进阶选项
- 设计热词加权解码（Seaco-Paraformer Hotword Boosting）：将高频用语与专有词汇构建为热词表，在解码阶段强制提升权重，解决同音字错误（对应构音障碍者"发音接近但字形差异大"的识别失败场景）
- 探索方言专项路由：增加 LID 语种识别模块，标准普通话走 Whisper，方言走 Paraformer-方言版或微调模型，适配构音障碍者伴随的口音/地方发音特征
- 评估模型迁移收益：Seaco-Paraformer（非自回归并行解码）推理速度约为 Faster-Whisper 的 **5–10 倍**（数据来源：仓库 `更换模型方案书.md`），显存需求降低约 80%，支持在同一 GPU 上与声纹识别服务共存部署
- 设计 AB 测试验证计划：准备普通话 / 重口音 / 纯噪音三类测试集，对比 CER（字错误率）与 RTF（实时率），量化热词对专有词汇召回率的提升效果

---

### 项目 4：边缘推理日志加密传输系统（loggrpc）

| 字段 | 内容 |
|------|------|
| **项目归属** | [填写：个人 / 实验室 / 公司名称] |
| **个人角色** | 后端工程师（系统设计、链路实现与联调） |
| **周期** | 2026.03 — [填写结束月份] |
| **技术栈** | Python · gRPC · MessagePack · Zstandard · AES-256-GCM · ECC 密钥交换 · Redis Stream · IoTDB · Prometheus |

**项目简介**  
将边缘端推理日志经序列化、压缩、加密后通过 gRPC 上传至服务端；服务端解密后写入 Redis Stream，消费者异步落库时序数据库（IoTDB）。系统具备端到端安全传输、背压控制、断点续传与可观测能力，适用于需要高可靠实时数据管道的 IoT / 边缘计算场景。

**工作要点**
- 设计并落地端到端加密传输链路：JSON → MessagePack 序列化 → Zstandard 压缩 → AES-256-GCM 加密 → gRPC 上传，密钥通过 ECC 动态下发并支持多版本轮换
- 接收与写入解耦：服务端解密后写入 Redis Stream，消费者异步消费并写入 IoTDB 时序库，支持背压控制与消费者水平扩展
- 可靠性保障：Offset 断点续传（客户端重启后从断点继续）、时间戳单调性校验、Nonce 防重放攻击
- 可观测体系：Prometheus 指标采集（QPS / 延迟 / 队列堆积 / 失败率），Stream 监控与死信队列告警，定位链路瓶颈

---

### 项目 5：推理日志 JSON 批处理与 IoTDB 全链路融合

| 字段 | 内容 |
|------|------|
| **项目归属** | [填写：个人 / 实验室 / 公司名称] |
| **个人角色** | 数据工程师（预处理管线设计与全链路联调） |
| **周期** | 2026.03（工作总结记录约 30h） |
| **技术栈** | Python · YAML 映射配置 · IoTDB · Redis Stream · gRPC 客户端 · AES-GCM / ECC 加密传输链路 |

**项目简介**  
将原始推理日志 JSON 经规范化、校验、分割处理后，接入「客户端 → 加密传输 → 服务端 → Redis Stream → 消费者 → IoTDB」正式写入链路，完成批处理预处理管线与时序库全链路的联调闭环。

**工作要点**
- 搭建预处理管线：校验修正 → 合并 → 按规则分割 → 批量输出，统一字段（`model_name` / `model_id` / `device_id` / `sn` / `camera_id` / `tag_id`）及图片路径一致性，避免下游入库时的字段歧义
- 明确链路复用与入库粒度：确认沿用 MsgPack + Zstd + AES-GCM 及 ECC 密钥交换，入库粒度为**分割后的单条 JSON**时序，而非整包重复写入
- IoTDB 工程闭环：路径规范与特殊字符清洗，配合 writer / query / verifier / delete 脚本实现完整数据生命周期管理；排查"已写入但查不到"等路径与查询一致性问题
- 全链路联调验证：output 文件与库内 `log_data` 一致性校验，错误数据删除后重发与复跑，保证批处理结果的幂等性

---

### 项目 6：实验室智能监控系统 V1（lab-monitor-v1）

| 字段 | 内容 |
|------|------|
| **项目归属** | [填写：个人 / 实验室 / 公司名称] |
| **个人角色** | 全栈工程师（后端 + 前端 + 业务规则） |
| **周期** | [填写：起止月份] |
| **技术栈** | Python 3.10 · FastAPI · Uvicorn · OpenCV · Ultralytics YOLOv8（双模型：通用 COCO + 烟火检测）· ByteTrack · SQLite · SQLAlchemy · Vue3 + Element Plus（CDN）· WebSocket |

**项目简介**  
单进程多路摄像头接入与实时预览系统，覆盖 RTSP / 本地上传、目标检测与跟踪、多类型安防与实验室规则告警、截图短视频存证与报表导出全链路。核心并发 worker 架构与事件驱动告警模式可直接迁移至多路音频流 + ASR + 声纹的并发处理框架。

**工作要点**
- 多路 `camera_worker` 并发架构：抽帧 / 推理 / 预览帧叠加 / ROI 判断（消防通道、窗户等多边形区域）/ 告警去重与截图短视频存证，分层处理以控制资源占用并保持实时性
- 多类型业务规则告警：夜间闯入、超员、人员 / 电脑超时、烟火检测、吸烟启发式、门禁 / 垃圾桶 / 水槽等 ROI + MOG 背景减除启发式规则，支持现场标定与热更新
- Web 管理界面：摄像头与 ROI 配置管理、告警记录筛选与 CSV / Excel 导出、日 / 周 / 月统计报表导出；`/ws/alerts` WebSocket 端点实现紧急告警实时推送
- 数据层与接口稳定性：SQLite 持久化 + SQLAlchemy ORM；静态资源采用按需路由，规避 `StaticFiles` 全局挂载导致 API 405 的问题

---

## 二、一页纸压缩版（可直接复制到简历）

> 每个项目 2 行简介 + 3 条 bullet，建议按比赛招募方向的优先级排序选用。音频/ASR方向优先取项目一至三；工程能力展示取项目四至六。

---

**项目一：实时语音转写与流式字幕 / 翻译 / 总结系统**  
技术栈：Python · FastAPI · WebSocket · Faster-Whisper · Silero VAD · ffmpeg · Qwen2.5（Ollama）  
面向实时沟通场景，构建"音频解码 → VAD 切分 → ASR → 翻译/总结 → 字幕流推送"端到端链路，支持构音障碍场景下的热词扩展与声纹过滤接口预留。

- 实现完整 ASR/VAD 实时处理管线，优化重叠切片策略与动态静音阈值，降低模糊发音漏检率，适配嘈杂环境下的非标准语音输入
- 后端集成自定义词表与提示词模板（prompt engineering），提升高频专有词汇的识别召回率，可扩展为热词加权解码与个性化词表
- 多会话管理 + 断线重连 + 异常兜底，服务层预留声纹过滤与降噪前处理热插拔接口，架构可直接适配羽言构音障碍辅助沟通场景

---

**项目二：声纹识别与说话人区分系统（Remote-V2 · V14.2）**  
技术栈：Python · PyTorch · ERes2NetV2 · Silero VAD · AS-Norm · FastAPI · CUDA  
基于声纹嵌入 + AS-Norm 自适应归一化的说话人验证系统，支持多人混说区分与嘈杂环境下的声纹过滤，适用于羽言产品的个人声纹注册与辅助沟通身份绑定。

- VAD 优先策略提取有效语音段 + Kaldi fbank 特征提取（全局 CMVN），保证噪声环境下特征稳定性；批量推理速度提升 10–50 倍（来源：仓库 V14.0 说明）
- AS-Norm 自适应分数归一化校正跨设备 / 跨环境评分漂移，降低误识率，支持轻量化部署（约 1.5–2 GB 显存）
- 提供健康检查脚本与 FastAPI 服务接口，模型与推理前端特征对齐，支持音频文件与实时流两种输入模式

---

**项目三：ASR 精度提升与模型迁移（嘈杂 / 方言 / 热词场景）**  
技术栈：Python · Faster-Whisper · FunASR（Seaco-Paraformer）· FSMN-VAD · LLM RAG（Qwen2.5）  
针对构音障碍者"发音模糊 + 嘈杂环境 + 专有词汇识别率低"痛点，研究 7 类 ASR 精度提升方案，设计 Seaco-Paraformer 热词加权解码迁移路径，推理速度提升 5–10 倍，显存降低约 80%（来源：仓库更换模型方案书）。

- VAD 切片优化 + Prompt 工程（低成本必选）：动态阈值防止短促发音被截断，自定义词表提升专有词召回率
- Seaco-Paraformer 热词加权解码：解码阶段强制提升高频用语权重，解决同音字错误，适配构音障碍者近似发音场景
- 方言路由 + LLM 语义纠错兜底：LID 模块分流方言模型，LLM + RAG 对 ASR 输出进行上下文修正，全链路覆盖"听不清 / 听不懂 / 写不对"三类错误

---

**项目四：边缘推理日志加密传输系统（loggrpc）**  
技术栈：Python · gRPC · MessagePack · Zstandard · AES-256-GCM · ECC · Redis Stream · IoTDB · Prometheus  
将边缘端推理日志经序列化、压缩、加密后通过 gRPC 上云，服务端解密入 Redis Stream，消费者异步落库时序数据库，实现低耦合高可靠数据管道。

- 端到端加密链路：JSON → MsgPack → ZSTD → AES-GCM，ECC 密钥动态下发与多版本轮换，保障传输安全
- 可靠性机制：Offset 断点续传、时间戳校验、Nonce 防重放；gRPC + Redis Stream 解耦接收与写入，支持背压控制
- Prometheus 可观测：QPS / 延迟 / 队列堆积 / 失败率全链路指标，死信队列告警辅助快速定位瓶颈

---

**项目五：推理日志 JSON 批处理与 IoTDB 全链路融合**  
技术栈：Python · YAML 映射配置 · IoTDB · Redis Stream · gRPC 客户端 · AES-GCM 加密传输链路  
构建推理日志预处理管线，完成 JSON 规范化分割后接入正式加密传输链路，实现与 IoTDB 的全链路联调闭环。

- 预处理管线：校验修正 → 合并 → 按规则分割 → 批量输出，统一多字段与图片路径一致性，保障下游入库无歧义
- IoTDB 工程闭环：路径规范与特殊字符清洗，writer / query / verifier / delete 脚本完整生命周期管理
- 全链路联调验证：output 与库内 `log_data` 一致性校验，支持错误数据删除后重发与幂等复跑

---

**项目六：实验室智能监控系统 V1（lab-monitor-v1）**  
技术栈：Python 3.10 · FastAPI · OpenCV · YOLOv8（双模型）· ByteTrack · SQLite · Vue3 · WebSocket  
单进程多路摄像头实时监控系统，覆盖检测 / 跟踪 / 多规则告警 / 存证 / 报表全链路；并发 worker 模式可复用于多路音频流 + ASR 并发架构。

- 多路 `camera_worker` 并发架构：抽帧 / 推理 / ROI 判断 / 告警去重与存证分层处理，控制资源占用并保持实时性
- 多类型业务规则告警（夜间闯入 / 超员 / 烟火 / 吸烟 / 门禁等）+ WebSocket 实时推送；CSV / Excel 报表导出
- SQLite + SQLAlchemy 持久化，静态资源按需路由规避 API 405，前后端联调稳定交付

---

## 三、比赛对齐说明（羽言·构音障碍语音辅助）

以下说明本文档如何对应「融光智能·羽言」招募方向中的核心需求：

| 比赛需求关键词 | 对应项目与能力 |
|---|---|
| **构音障碍人群 / 发音模糊** | 项目1 VAD 切片优化，动态阈值防漏检；项目3 热词加权与方言路由 |
| **ASR / VAD** | 项目1 Faster-Whisper + Silero VAD 全链路；项目3 Seaco-Paraformer + FSMN-VAD 迁移方案 |
| **实时交互** | 项目1 WebSocket 字幕流推送，多会话管理，低延迟优化 |
| **嘈杂环境** | 项目2 AS-Norm 评分归一化，VAD 优先策略；项目3 AI 降噪 / 人声分离方案分析 |
| **声纹过滤** | 项目2 ERes2NetV2 声纹识别，说话人区分；项目1 声纹过滤接口预留 |
| **热词 / 个性化词表** | 项目1 prompt 工程 + 词表扩展接口；项目3 Seaco-Paraformer 热词加权解码 |
| **音视频工程 / 实时流处理** | 项目1 ffmpeg 解码；项目6 多路 camera_worker 并发架构（可迁移至音频） |
| **可靠数据管道 / 边缘计算** | 项目4 gRPC + Redis Stream + IoTDB 加密传输；项目5 批处理管线与全链路联调 |
| **软件工程落地** | 项目1/2/6 均含 FastAPI 服务化、健康检查、异常兜底等生产级工程实践 |

---

## 四、使用建议

- **选项目数量**：比赛简历一般放 2–3 个项目最佳；音频/ASR方向优先选项目1、3；有声纹能力则加项目2；工程能力补充可选项目4或6。
- **项目合并**：若项目4与项目5属同一工作阶段，可合并为「边缘日志加密传输与 IoTDB 全链路融合」，子模块分别写"加密传输链路"与"批处理与入库"；若项目1与项目3属同一阶段，可合并为「实时语音处理与 ASR 精度优化」。
- **量化补充**：如后续获得真实指标（端到端延迟 ms、WER 对比、并发会话数、日志处理量级等），可在实际面试材料中补充；简历正文仅引用仓库有明确记录的数值。
- **敏感信息**：不写具体 IP、内网服务地址、密钥、证书路径；技术名词与仓库公开 README 保持一致。
- **岗位侧重**：
  - **算法方向**：优先展示项目1（ASR/VAD pipeline）、项目3（模型选型与热词）
  - **软件/后端方向**：优先展示项目1（WebSocket 服务）、项目4（高可靠数据管道）、项目6（并发 worker）
  - **全栈/工程方向**：所有项目均可保留，突出端到端交付能力
- **填写前检查**：提交前请将所有 `[填写]` 占位符替换为真实的单位名称、起止日期等，避免占位符出现在正式简历中。
