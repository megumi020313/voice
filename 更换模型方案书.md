这是一份为您量身定制的**《ASR 模型迁移与热词增强实施方案书》**。

本方案旨在将原有的 **Faster-Whisper + Silero VAD** 架构，平滑迁移至阿里 **FunASR (Seaco-Paraformer)** 架构，重点解决**“菜名识别不准”**和**“四川话兼容性”**问题。

---

# 项目实施方案：Seaco-Paraformer 模型迁移与热词增强

## 1. 核心变更概述

| 维度 | 原方案 (Current) | **新方案 (Target)** | 变更收益 |
| :--- | :--- | :--- | :--- |
| **核心模型** | Faster-Whisper (Large-v3) | **Seaco-Paraformer (Large-热词版)** | 支持显式热词干预，中文/方言理解更强 |
| **VAD 模块** | Silero VAD | **FSMN-VAD (阿里自研)** | 与 Paraformer 深度耦合，切分更精准，减少丢字 |
| **标点恢复** | (未明确/Whisper自带) | **CT-Transformer** | 专业的中文标点恢复，提升语义可读性 |
| **推理架构** | Autoregressive (逐字生成) | **Non-Autoregressive (并行生成)** | **推理速度提升 5-10 倍**，显著降低延迟 |
| **定制化** | 仅靠 Prompt 暗示 | **热词权重 (Hotword Boosting)** | 强制提升菜单词汇权重，解决“同音不同字” |

---

## 2. 环境准备与依赖安装

**前提**：确保服务器已安装 Python 3.8+ 和 PyTorch (建议 1.13+，支持 CUDA)。

### 2.1 安装 FunASR 及相关库
```bash
# 卸载可能冲突的旧库 (可选)
pip uninstall whisper faster-whisper

# 安装阿里 FunASR 核心库
pip install funasr

# 安装 ModelScope (用于自动下载模型)
pip install modelscope

# 安装其他音频处理依赖
pip install torchaudio
```

---

## 3. 详细代码实现方案

我们将封装一个 `HotpotASR` 类，实现**模型加载**、**热词动态配置**和**推理服务**。

### 3.1 核心代码 (`asr_engine.py`)

```python
import os
from funasr import AutoModel

class HotpotASR:
    def __init__(self, device="cuda"):
        """
        初始化 ASR 引擎，加载声学模型(ASR)、语音活动检测(VAD)和标点模型(PUNC)
        """
        print("正在加载 Seaco-Paraformer 模型...")
        
        # 模型 ID 定义
        self.model_id = "iic/speech_seaco_paraformer_large_asr_nat-zh-cn-16k-common-vocab8404-pytorch"
        self.vad_id = "iic/speech_fsmn_vad_zh-cn-16k-common-pytorch"
        self.punc_id = "iic/punc_ct-transformer_zh-cn-common-vocab272727-pytorch"
        
        # 加载全链路模型
        # disable_update=True 防止每次启动都检查更新，加快启动速度
        self.model = AutoModel(
            model=self.model_id,
            vad_model=self.vad_id,
            punc_model=self.punc_id,
            device=device,
            disable_update=True 
        )
        
        print("模型加载完成。")

    def build_hotwords(self, menu_list, dialect_mapping=None):
        """
        构建热词字符串。
        :param menu_list: 标准菜单列表 ['毛肚', '鸭肠']
        :param dialect_mapping: 方言映射字典 {'niu rou': '牛肉'} (可选)
        :return: 格式化后的热词字符串
        """
        # 1. 基础菜单热词
        hotwords = list(menu_list)
        
        # 2. (高阶技巧) 添加四川话同音映射
        # 如果发现某些词老是识别错，可以在这里手动加“错别字”作为热词，
        # 虽然 Seaco 是基于字级别的，但增加同音字的权重有助于模型锁定发音。
        if dialect_mapping:
            hotwords.extend(dialect_mapping.keys())
            
        # FunASR 的热词格式通常是空格分隔的字符串
        return " ".join(hotwords)

    def transcribe(self, audio_path, hotword_str=""):
        """
        执行语音转文字
        :param audio_path: 音频文件路径 或 wav url
        :param hotword_str: 预处理好的热词字符串
        """
        try:
            # param_dict 可以调节热词的权重，默认是 1.0
            # 某些版本支持 hotword_weight 参数，若不支持则仅传 hotword
            res = self.model.generate(
                input=audio_path,
                hotword=hotword_str,
                batch_size_s=300,  # 批处理时长，根据显存调整
                use_itn=True       # 开启逆文本标准化 (把"一二三"转为"123")
            )
            
            # res 结构通常为List: [{'key': 'wav_name', 'text': '识别结果'}]
            if res and len(res) > 0:
                return res[0]['text']
            return ""
            
        except Exception as e:
            print(f"识别出错: {e}")
            return None

# --- 使用示例 ---
if __name__ == "__main__":
    # 1. 初始化引擎
    asr_engine = HotpotASR(device="cuda")

    # 2. 定义火锅店菜单 (建议从数据库动态读取)
    hotpot_menu = [
        "毛肚", "千层肚", "九尺鹅肠", "贡菜", "耗儿鱼", 
        "唯怡豆奶", "红糖糍粑", "鸳鸯锅", "加汤", "微辣"
    ]
    
    # 3. 构建热词串
    # 技巧：为了解决四川话，可以加入一些方言发音的近似字，强行纠正
    # 但 Seaco 模型本身对同音字有很强的纠错能力，先只传标准菜名即可
    hotword_str = asr_engine.build_hotwords(hotpot_menu)
    print(f"当前生效热词: {hotword_str[:50]}...")

    # 4. 推理
    audio_file = "test_sichuan.wav" # 替换为你的测试音频
    text = asr_engine.transcribe(audio_file, hotword_str)
    
    print(f"识别结果: {text}")
```

---

## 4. 针对四川话与精度的专项优化策略

更换模型只是第一步，要达到最佳效果，请执行以下配置：

### 4.1 热词配置技巧 (解决“写不对”)
Seaco-Paraformer 的强大之处在于它会给热词加权。
*   **标准菜名**：必须全部加入。
*   **服务术语**：加入“加汤”、“买单”、“拿个漏勺”、“打个干碟”。
*   **方言修正（关键）**：
    如果测试发现“牛肉”老是被识别成“刘肉”（四川话 n/l 不分），请在热词表中**同时加入** `牛肉` 和 `刘肉`。
    *   *逻辑*：虽然模型识别出了“刘肉”，但后端业务逻辑可以通过模糊匹配，把“刘肉”自动修正回“牛肉”。**让 ASR 识别出它听到的音，比强行让它猜对字更重要。**

### 4.2 VAD 参数微调 (解决“切不准”)
FunASR 使用的 FSMN-VAD 默认参数比较保守。如果火锅店背景太吵，导致人声被切掉，可以在 `generate` 函数中调整 VAD 参数：

```python
res = self.model.generate(
    input=audio_path,
    hotword=hotword_str,
    # VAD 参数微调
    vad_kwargs={
        "max_single_segment_time": 60000, # 最大切片时长
        "vad_tail_silence_filter": False, # 是否过滤尾部静音
        # 如果发现漏掉短词，可以尝试调整阈值 (需查看源码确认具体参数名，通常默认即可)
    }
)
```

---

## 5. 部署与性能评估

### 5.1 资源占用预估
*   **显存 (VRAM)**：Paraformer-Large 非常轻量。
    *   加载模型约占用 **1.5GB - 2GB** 显存。
    *   相比 Whisper Large-v3 (约 10GB)，显存需求降低了 80%。
    *   这意味着你可以在一张显卡上部署多个 worker，或者换用更便宜的显卡（如 T4, 甚至 3060）。
*   **延迟 (Latency)**：
    *   10秒音频推理时间：Whisper 约需 1-2秒，Paraformer 仅需 **0.2-0.3秒**。

### 5.2 验证测试计划
在正式上线前，请进行以下测试（AB Test）：

1.  **准备测试集**：
    *   20条标准普通话点菜录音。
    *   20条**重口音四川话**点菜录音。
    *   10条纯噪音频（火锅沸腾声）。
2.  **对比指标**：
    *   **CER (字错误率)**：重点关注菜名实体。
    *   **RTF (实时率)**：处理时长 / 音频时长。
3.  **预期结果**：
    *   普通话精度：与 Whisper 持平。
    *   四川话精度：**提升 20% 以上**。
    *   菜名召回率：**提升 40% 以上**（归功于热词）。

---

## 6. 总结与下一步

**方案结论**：
切换到 `iic/speech_seaco_paraformer_large_asr_nat-zh-cn-16k-common-vocab8404-pytorch` 是目前针对您项目**性价比最高、改动风险最小、效果最显著**的方案。

**下一步行动**：
1.  在开发环境运行上述 `asr_engine.py` 代码。
2.  录制几句四川话：“老板，来份毛肚，搞快点哈”，测试热词生效情况。
3.  如果效果满意，即可替换现有的 Whisper 服务接口。