---
description: 任务创建、变更日志、功能点完成流程，包括文档和版本管理。核心开发流程，强制要求在任何开发工作前建立需求文档和任务
alwaysApply: true
---
# 任务管理与功能点完成流程

## 任务管理规则

### 创建任务
1. 文件夹：`docs/optimization/tasks/YYYY-MM/task-name/`
2. 文件：`README.md` + `CHANGELOG.md`
3. 更新：`优化状态.md` + 全局 `CHANGELOG.md`

### 变更日志规则

#### 全局变更日志
每次代码修改（包括项目代码与测试代码），在 `docs/changelog/` 下创建新的 `CHANGLOG-时间戳.md` 文件

**获取时间戳**：
- Windows (PowerShell): `powershell -Command "Get-Date -Format 'yyyyMMdd-HHmmss'"`
- Mac/Linux: `date +"%Y%m%d-%H%M%S"`

**文件命名**：`CHANGLOG-YYYYMMDD-HHMMSS.md`（精确到秒）

**记录内容**：
- 变更概要
- 影响模块（utils/common/modules/core/tests）
- 关键文件
- 函数索引更新
- 提示词/规则变更

**参考模板**：`docs/changelog/CHANGLOG_TEMPLATE.md`

#### 任务变更日志
如涉及某个优化任务，同时更新对应任务目录下的 `CHANGELOG.md`

**原则**：实现"任务内 + 全局"双视角，格式统一，记录详细

## 功能点完成流程规则

### 核心原则
**完成一个功能点时，必须自动完成以下五项工作**（文档 + 更新日志 + YesDev同步是强制要求）：
1. ✅ **创建或更新相关文档**：新增功能必须创建设计文档，修改功能必须更新相关文档
2. ✅ **更新YesDev任务状态**：将相关任务状态更新为"已完成"，记录完成时间
3. ✅ **创建详细变更日志**：记录本次变更的详细信息，包含文档链接
4. ✅ **更新全局CHANGELOG**：在项目根目录的`docs/CHANGELOG.md`中添加版本条目，包含文档链接
5. ✅ **推进项目版本号**：确定并更新版本号（遵循版本号管理规范）

### 完成流程步骤

#### 步骤1：获取时间戳
```bash
# Windows (PowerShell)
powershell -Command "Get-Date -Format 'yyyyMMdd-HHmmss'"

# Mac/Linux
date +"%Y%m%d-%H%M%S"
```

#### 步骤2：确定版本号
1. **检查当前最新版本**：
   - 查看 `CHANGELOG.md` 文件顶部的最新版本号
   - 或使用：`git tag --list | Select-String "alpha-0.17.3"`（PowerShell）
2. **确定下一个版本号**：
   - 新功能 → 增加次版本号（Y）
   - Bug修复 → 增加次版本号（Y）或使用 `alpha-X.Y.1` 格式
   - 小修复 → 增加次版本号（Y）或使用 `alpha-X.Y.1` 格式
   - 确保版本号递增且不重复
3. **版本号格式**：`alpha-X.Y`（当前项目使用简化格式）
   - 示例：如果最新是 `alpha-1.0`，新功能可能是 `alpha-1.1`
   - 当前为空仓库，初始版本为 `alpha-1.0`

#### 步骤3：创建或更新相关文档
**⚠️ 重要：必须先创建文档，再创建变更日志！**

1. **判断是否需要创建文档**：
   - 新增功能 → **必须**创建设计文档
   - 复杂修改 → **必须**创建分析文档或更新设计文档
   - 简单修复 → 可只更新变更日志，但建议更新相关文档
2. **文档归档位置**：
   - 设计文档：按模块归档到对应子目录（`docs/1-设计/`）
   - 需求文档：`docs/200-需求管理/`
   - Bug文档：`docs/201-Bug修复任务管理/`
   - 优化文档：`docs/202-优化任务管理/`
3. **文档内容要求**：
   - 模块层级说明（utils/common/modules/core）
   - 设计思路或问题分析
   - 关键实现说明
   - **任务定义**（模块、函数、功能）- 如适用
   - **模块化设计**（复用模块、新增模块、函数复用）- 如适用
   - 使用说明（如适用）
   - 相关文档链接
4. **更新文档索引**：
   - 如创建新文档，更新 `docs/README.md`
   - 更新相关模块索引（如有）
5. **详细规范**：参见 [文档管理规范](文档管理规范.mdc)

#### 步骤4：更新YesDev任务状态
**⚠️ 重要：必须先更新YesDev任务，再创建变更日志！**

1. **更新任务状态**：
   - 使用 `mcp_yesdev-mcp-server_update_task` 函数
   - 将任务状态更新为 `2000`（已完成）
   - 记录实际完成时间（`real_task_time`）

2. **更新本地需求文档**：
   - 在 `docs/200-需求管理/` 对应需求文档中更新任务状态
   - 记录任务完成时间

3. **检查需求完成情况**：
   - 如果需求下的所有任务都已完成，更新需求状态为"已完成"
   - 使用 `mcp_yesdev-mcp-server_update_need` 更新需求状态

#### 步骤5：创建详细变更日志
1. **文件路径**：`docs/changelog/CHANGLOG-YYYYMMDD-HHMMSS.md`
2. **文件内容**：参考 `docs/changelog/CHANGLOG_TEMPLATE.md`
3. **必须包含**：
   - 版本信息（版本号、变更时间）
   - 变更概要
   - 影响模块
   - **文档链接**（如创建了新文档，必须包含链接）
   - 关键文件修改说明
   - 新增功能说明（如有）
   - 问题修复说明（如有）
   - 性能优化说明（如有）
   - 向后兼容性说明

#### 步骤5：更新函数索引（如适用）
如果新增或修改了公共函数：
1. **检查是否需要更新**：
   - 新增公共函数 → 需要更新
   - 修改公共函数接口 → 需要更新
   - 删除公共函数 → 需要更新
2. **更新位置**：
   - 主索引：`docs/design/index/函数索引总览.md`
   - 模块索引：`docs/design/modules/[模块]_函数索引.md`
   - 服务索引：`docs/design/services/[服务]_函数索引.md`
3. **更新内容**：
   - 函数签名、功能描述、参数说明
   - 使用示例和依赖关系
   - 文件位置和行号
4. **分类规则**：
   - core：用户管理、权限、基础功能
   - backend：后端业务逻辑
   - frontend：前端组件逻辑
   - utils：通用工具函数
   - modules：具体业务模块
   - services：业务服务层

#### 步骤6：更新全局CHANGELOG.md
1. **位置**：项目根目录 `CHANGELOG.md`
2. **操作**：在文件**最顶部**添加新版本条目
3. **格式**：
   ```markdown
   ## [alpha-X.Y.Z.N] - YYYY-MM-DD HH:MM:SS - 变更概要标题

   ### 变更概要

   [简要描述本次变更的主要内容]

   **影响模块**：
   - [模块名]: [模块描述]

   **关键变更**：
   - [变更点1]
   - [变更点2]

   **函数索引更新**：新增/修改 [X] 个函数，详见 `docs/design/index/函数索引总览.md`
   **详细变更日志**：`docs/changelog/CHANGLOG-YYYYMMDD-HHMMSS.md`

   ---
   ```

#### 步骤7：更新技术债文档（如适用）
如果本次变更涉及技术债文档中的任务：
1. **文件路径**：`docs/design/technical_debt/技术债与改进项列表.md`
2. **更新内容**：
   - 将任务状态从"待实施"改为"✅ 已完成"
   - 添加完成时间
   - 更新相关文件列表（如有新增文件）
   - 添加完成说明和变更日志链接

#### 步骤8：更新函数索引（如适用）
如果新增或修改了公共函数：
1. **检查是否需要更新**：
   - 新增公共函数 → 需要更新
   - 修改公共函数接口 → 需要更新
   - 删除公共函数 → 需要更新
2. **更新位置**：
   - 全局索引：`docs/design/index/函数索引总览.md`
   - 模块索引：对应的模块索引文件（如 `docs/design/core_函数索引.md`）
3. **在变更日志中记录**：在详细变更日志的"函数索引更新"部分说明

### 完成检查清单

在完成功能点后，必须确认以下所有项：

- [ ] ✅ **已创建或更新相关文档**（新增功能必须创建，复杂修改建议创建）
- [ ] ✅ **已更新YesDev任务状态**（任务状态更新为"已完成"，记录完成时间）
- [ ] ✅ **已更新本地需求文档**（任务状态和完成时间已同步）
- [ ] ✅ 已获取时间戳
- [ ] ✅ 已确定版本号（递增且不重复）
- [ ] ✅ 已创建详细变更日志文件（`docs/changelog/CHANGLOG-YYYYMMDD-HHMMSS.md`），包含文档链接
- [ ] ✅ 已更新全局CHANGELOG.md（在顶部添加新版本条目），包含文档链接
- [ ] ✅ 已更新文档索引（如创建新文档，更新 `docs/README.md`）
- [ ] ✅ 已更新技术债文档（如涉及任务）
- [ ] ✅ 已更新函数索引（如涉及公共函数）
- [ ] ✅ 变更日志中的版本号与CHANGELOG.md一致
- [ ] ✅ 所有文档路径引用正确
- [ ] ✅ 文档已归档到正确的模块子目录

### 自动化提示

**当AI助手完成功能点开发后，应自动执行以下操作**：

1. **创建或更新相关文档**（⚠️ 必须先完成）：
   - 判断是否需要创建文档（新增功能必须，复杂修改建议）
   - 按模块归档到对应子目录（参考文档组织规范）
   - 文档必须包含：模块层级、设计思路、关键实现、使用说明
   - 更新文档索引（`docs/README.md`）

2. **更新YesDev任务状态**（⚠️ 必须完成）：
   - 调用 `mcp_yesdev-mcp-server_update_task` 更新任务状态为"已完成"（2000）
   - 记录实际完成时间（`real_task_time`）
   - 更新本地需求文档中的任务状态和完成时间
   - 检查需求是否全部完成，如完成则更新需求状态

3. **获取时间戳**：
   ```bash
   powershell -Command "Get-Date -Format 'yyyyMMdd-HHmmss'"
   ```

4. **检查当前版本号**：
   - 读取 `docs/CHANGELOG.md` 顶部获取最新版本号
   - 确定下一个版本号

5. **创建变更日志**：
   - 使用时间戳创建 `docs/changelog/CHANGLOG-YYYYMMDD-HHMMSS.md`
   - 填写完整的变更信息
   - **必须包含文档链接**（如创建了新文档）
   - **必须包含YesDev任务更新信息**

6. **更新全局CHANGELOG**：
   - 在 `docs/CHANGELOG.md` 顶部添加新版本条目
   - 包含版本号、时间、概要、**文档链接**

7. **更新技术债文档**（如适用）：
   - 更新任务状态为"已完成"
   - 添加完成时间和说明

8. **更新函数索引**（如适用）：
   - 记录新增或修改的公共函数

### 示例流程

**场景**：完成"采购申请表单"功能点

1. **创建或更新文档**：创建 `docs/1-设计/采购申请表单设计.md`
2. **更新YesDev任务**：调用 `mcp_yesdev-mcp-server_update_task` 更新任务状态为"已完成"，记录完成时间
3. **更新本地需求文档**：在 `docs/200-需求管理/采购表单需求.md` 中更新任务状态
4. **获取时间戳**：`20251215-171549`
5. **确定版本号**：查看CHANGELOG.md，最新是`alpha-1.0`，新功能应为`alpha-1.1`
6. **创建变更日志**：`docs/changelog/CHANGLOG-20251215-171549.md`
7. **更新全局CHANGELOG**：在顶部添加`alpha-1.1`条目
8. **完成**：所有文档已更新，YesDev任务已同步，版本已推进

