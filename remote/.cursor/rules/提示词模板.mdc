---
description: AI 助手使用的提示词模板和快速参考，包括模块选择、依赖检查、导入路径。AI 需要快速参考时使用。
alwaysApply: false
---
# 提示词模板

## 模块选择
根据功能选择模块：
- 纯工具 → `backend/utils/`（纯工具函数，无业务逻辑）
- 通用组件 → `backend/common/`（业务通用组件，多模块复用）
- 数据存储 → `backend/data/`（SQLAlchemy模型）
- 完整业务 → `backend/modules/`（如approval、integration）
- 系统协调 → `backend/scheduler/`（系统调度任务）
- 主业务模块 → `backend/`（MVC架构：controller/schema/service）
- 配置管理 → `backend/config/`（配置管理）
- 命令行 → `scripts/`（命令行脚本工具）
- 测试代码 → `tests/`
- 开发辅助工具 → `tools/`（本项目暂无，可创建）

## 任务创建
创建优化任务：
- docs/optimization/tasks/YYYY-MM-任务名称/（使用中文命名）
- README.md + CHANGELOG.md
- 更新OPTIMIZATION_STATUS.md

## 依赖检查
检查依赖：
✅ tests → backend/scheduler → backend/modules → {backend/data, backend/common} → backend/utils
✅ tests → tests/utils/（测试工具）
✅ tools → {backend/modules, backend/common, backend/utils}（工具可以依赖项目模块）
❌ 禁止反向和循环依赖
❌ tests → scripts/（测试不应依赖脚本）
❌ tests → tools/（测试不应依赖工具）
❌ backend模块 → tools/（项目模块不应依赖工具）
❌ backend模块 → tests/（项目模块不应依赖测试）

## 测试工具分类
- 测试工具函数（可被测试导入）→ tests/utils/
- 独立测试脚本（可直接运行）→ scripts/test_*.py 或 tests/scripts/
- 纯工具函数（无状态、无业务逻辑）→ src/utils/
- 命令行工具（非测试）→ scripts/
- 开发辅助工具（Mock API、检测工具等）→ tools/

## 导入路径
- ✅ 必须使用绝对路径：`from backend.service import ProcurementService` 或 `from backend.data.procurement_models import ProcurementApply`
- ✅ 测试工具导入：`from tests.test_services_simple import TestProcurementApprovalService`
- ❌ 禁止相对路径：`from .service import Service` 或 `from ..models import Model`
- 所有导入必须使用模块名开头的绝对路径（如 `backend.`、`backend.data.`、`backend.modules.`、`tests.`）

## Windows 编码处理
- ✅ 所有包含 emoji 或特殊字符的脚本必须在开头设置 UTF-8 编码
- ✅ 所有启动子进程的代码必须传递 UTF-8 环境变量
- ✅ 所有日志输出应使用 `errors='replace'` 处理编码错误
- 详细规则参考：`.cursor/rules/终端环境与编码处理.md`

## 功能点完成流程（核心：文档 + 更新日志）
**⚠️ 重要：完成任务后必须创建文档和更新日志，这是强制要求！**

完成功能点后必须自动完成：
1. **创建或更新相关文档**：
   - 新增功能 → 创建设计文档（`docs/design/模块名/功能名.md`）
   - 修改功能 → 更新相关设计文档
   - 复杂变更 → 创建分析文档（`docs/analysis/问题分析.md`）
   - 文档必须包含：模块层级、设计思路、关键实现、使用说明
2. 获取时间戳：`powershell -Command "Get-Date -Format 'yyyyMMdd-HHmmss'"`
3. 确定版本号：查看CHANGELOG.md最新版本，递增版本号
4. **创建变更日志**：`docs/changelog/CHANGLOG-YYYYMMDD-HHMMSS.md`
   - 必须包含：变更概要、影响模块、关键文件、文档链接
5. **更新全局CHANGELOG**：在顶部添加新版本条目，包含文档链接
6. 更新技术债文档：如涉及任务，更新状态为"已完成"
7. 更新函数索引：如涉及公共函数，更新索引文档
8. **更新文档索引**：如创建新文档，更新 `docs/README.md` 和相关索引

**文档创建原则**：
- ✅ 新功能必须有设计文档
- ✅ 复杂修改必须有分析文档
- ✅ 所有文档必须归档到正确的模块目录
- ✅ 文档必须包含模块层级说明
- ❌ 禁止只写代码不写文档
- ❌ 禁止文档与代码不同步

- 详细流程参考：`.cursor/rules/任务管理与功能点完成流程.md`

## 文档命名与归档（按模块细分）
- 新增项目文档优先使用中文文件名，例如：`采购与资产管理模块实现总结.md`、`系统集成需求.md`
- **按模块细分归档**（重要：必须归档到对应模块子目录）：
  - **设计文档** → `docs/1-设计/`（架构设计、模块设计、实现总结）
  - **需求管理** → `docs/200-需求管理/`（需求文档、任务管理）
  - **变更日志** → `docs/changelog/`（变更日志文件）
  - **开发规范** → `docs/development/`（开发规范、流程文档，如需要）
  - **内部工具** → `docs/tools/`（工具使用说明，如需要）
  - **优化任务** → `docs/optimization/tasks/`（优化任务文档，如需要）
  - **分析报告** → `docs/analysis/`（问题分析、性能分析，如需要）
- 每份设计文档开头应注明涉及的模块层级（backend/utils/backend/common/backend/modules/backend/scheduler）
- 详细规范参考：`docs/development/文档组织规范.md`

## 函数索引维护
- 为便于大模型/开发者快速理解接口，关键函数应维护在函数索引文档中
- 全局索引：`docs/design/index/函数索引总览.md`
- 按模块拆分索引：
  - `docs/design/utils_函数索引.md`
  - `docs/design/common_函数索引.md`
  - `docs/design/modules_推理相关函数索引.md`
  - `docs/design/modules_RTSP相关函数索引.md`
  - `docs/design/core_函数索引.md`
- 新增或修改公共函数时需同步更新相关索引文件，并在本次CHANGLOG中记录
- 函数索引文件只描述接口，不引入实际代码依赖，属于文档层

## 大模型协作优化
- 优先查阅以下文档，减少无效token消耗：
  1. `docs/design/index/函数索引总览.md` 与对应模块索引文件
  2. `docs/changelog/` 下最近几次变更日志
  3. `docs/design/` 中相关架构设计文档
- 只有在索引信息不足时才展开阅读完整源码文件

## 文档引用路径更新
- **重命名或移动文件后**：必须更新所有引用该文件的文档中的路径
- **检查方法**：使用 `grep` 搜索旧路径，确保所有引用都已更新
- **常见引用位置**：
  - Markdown 文档中的链接（`[文本](路径)`）
  - 代码注释中的文档路径
  - README 文件中的文档索引
  - 变更日志中的文件路径
- **工具使用**：重命名文件时使用项目提供的工具（`scripts/rename_files.py` 或 `src/utils/file/rename_ops.py`），避免手动操作导致的编码问题

