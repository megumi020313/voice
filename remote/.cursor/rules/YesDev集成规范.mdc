---
description: YesDev MCP服务集成规范，需求管理、任务管理、问题跟踪的自动化流程。使用 YesDev 相关功能时使用。
alwaysApply: false
---
# YesDev集成规范

## 核心原则
- **自动同步**：本地需求文档与YesDev平台保持实时同步
- **双向更新**：需求状态变更时同步更新本地文档和YesDev平台
- **问题跟踪**：测试和开发过程中发现的问题自动在YesDev中创建任务
- **时间记录**：需求创建时间、任务完成时间必须准确记录

## YesDev项目信息
- **项目名称**：科研管理系统
- **MCP服务**：yesdev-mcp-server
- **默认需求创建时间**：2025-12-15 14:00（如未指定则使用此时间）

## 需求管理流程

### 创建需求
1. **在YesDev中创建需求**：
   - 使用 `mcp_yesdev-mcp-server_create_need` 函数
   - 项目ID：科研管理系统项目ID
   - 需求名称：清晰描述功能需求
   - 需求描述：使用HTML格式，详细说明需求内容
   - 创建时间：如未指定，使用默认时间（2025-12-15 14:00）

2. **在本地创建需求文档**：
   - 位置：`docs/200-需求管理/`
   - 文件命名：按功能模块分类，如 `采购表单需求.md`、`采购服务需求.md`
   - 内容要求：
     - 需求ID（YesDev中的需求ID）
     - 需求名称
     - 需求描述
     - 需求状态（待开发/开发中/已完成/已取消）
     - 负责人
     - 计划开始时间
     - 计划完成时间
     - **原子性拆分**（需求必须拆分为原子性需求）
     - **任务定义**（每个任务必须定义模块、函数、功能）
     - 关联任务列表
     - 相关文件列表
     - **模块化设计**（复用模块、新增模块、函数复用）
   - 详细规范：参见 [文档管理规范](文档管理规范.mdc)

3. **同步机制**：
   - 需求创建时：同时创建YesDev需求和本地文档
   - 需求更新时：同步更新YesDev和本地文档
   - 需求完成时：更新状态为"已完成"，记录实际完成时间

### 需求状态管理
- **待开发**：需求已创建，等待开发
- **开发中**：需求正在开发中
- **已完成**：需求已实现并通过验收（记录完成时间）
- **已取消**：需求已取消或废弃

## 任务管理流程

### 创建任务

#### 任务创建时机
- 需求分析完成后，为每个原子性需求创建任务
- 发现Bug时，立即创建Bug修复任务
- 发现优化点时，创建优化任务
- 需要编写文档时，创建文档任务

#### 任务粒度标准
- **合适粒度**：
  - 一个任务对应一个功能点或一个函数实现
  - 可以在1-3天内完成
  - 有明确的输入、输出和验收标准
  - 可以独立测试和验收
- **避免过小**：不要将单个函数调用拆分为多个任务
- **避免过大**：超过3天的任务应拆分为多个子任务

#### 任务创建步骤
1. **在YesDev中创建任务**：
   - 使用 `mcp_yesdev-mcp-server_create_task` 函数
   - 关联需求ID：任务必须关联到对应的需求
   - 任务标题：清晰描述任务内容，格式：`[功能模块] 功能描述`
   - 任务描述：使用HTML格式，必须包含：
     - 任务目标
     - 模块路径（如 `backend/service/`）
     - 函数签名（如 `def create_user(...) -> User:`）
     - 功能描述
     - 依赖关系
     - 验收标准
   - 任务类型：
     - 0: 其他
     - 1: UI设计
     - 2: 产品原型
     - 3: 技术开发（默认）
     - 4: 测试
     - 5: 会议
     - 6: 编写文档
     - 7: 调研
     - 8: 沟通
   - 任务状态：
     - 600: 待办（默认）
     - 1500: 进行中
     - 2000: 已完成
   - 任务工时：合理估算，单个任务推荐不超过4小时

2. **在本地需求文档中记录任务**：
   - 在对应需求文档的"关联任务"部分添加任务信息
   - 记录任务ID、任务标题、任务状态
   - 记录任务的模块、函数、功能定义

### 任务完成流程
1. **更新任务状态**：
   - 使用 `mcp_yesdev-mcp-server_update_task` 函数
   - 将任务状态更新为 `2000`（已完成）
   - 记录实际完成时间（`real_task_time`）
   - 更新任务描述，添加完成说明

2. **更新本地文档**：
   - 在需求文档中将任务状态更新为"已完成"
   - 记录任务完成时间
   - 更新相关文件列表（如有新增文件）

3. **检查需求完成情况**：
   - 如果需求下的所有任务都已完成，将需求状态更新为"已完成"
   - 记录需求完成时间

### 任务关联规则
- **需求关联**：每个任务必须关联到一个需求
- **依赖关联**：如果任务依赖其他任务，在任务描述中说明依赖关系
- **模块关联**：任务必须明确所属模块（backend/utils/、backend/common/、backend/modules/等）

### 任务状态流转
```
待办(600) → 进行中(1500) → 已完成(2000)
                ↓
             已取消（如需要）
```
- **待办(600)**：任务已创建，等待开始
- **进行中(1500)**：任务已开始开发
- **已完成(2000)**：任务已完成并通过验收
- **已取消**：任务已取消或不再需要（可选状态）

## 问题跟踪流程

### 问题分类
测试和开发过程中发现的问题，按类型在YesDev中创建任务：

1. **Bug（故障）**：
   - 任务类型：3（技术开发）
   - 任务标题格式：`[BUG] 问题描述`
   - 任务描述：详细描述问题现象、复现步骤、预期结果、实际结果
   - **本地文档**：必须在 `docs/201-Bug修复任务管理/` 创建Bug文档
   - 文档命名：`BUG-YYYYMMDD-序号.md`
   - 文档内容：Bug信息、问题分析、修复方案（模块、函数、修复内容）
   - 详细规范：参见 [文档管理规范](文档管理规范.mdc)

2. **优化（性能优化、代码优化）**：
   - 任务类型：3（技术开发）
   - 任务标题格式：`[优化] 优化描述`
   - 任务描述：说明优化目标、优化方案、预期效果
   - **本地文档**：必须在 `docs/202-优化任务管理/` 创建优化文档
   - 文档命名：`OPT-YYYYMMDD-序号.md`
   - 文档内容：优化信息、现状分析、优化方案（模块、函数、优化内容）、模块化设计
   - 详细规范：参见 [文档管理规范](文档管理规范.mdc)

3. **故障（系统故障、服务故障）**：
   - 任务类型：3（技术开发）
   - 任务标题格式：`[故障] 故障描述`
   - 任务描述：说明故障现象、影响范围、紧急程度

### 问题创建流程
1. **发现问题**：
   - 在测试或开发过程中发现问题
   - 记录问题详细信息（现象、复现步骤、环境等）

2. **在YesDev中创建问题任务**：
   - 使用 `mcp_yesdev-mcp-server_create_task` 函数
   - 选择合适的问题类型（bug/优化/故障）
   - 关联到相关需求（如有）
   - 设置任务优先级和计划完成时间

3. **记录问题信息**：
   - 在变更日志中记录问题创建信息
   - 在相关需求文档中记录问题（如适用）

## 开发工作流程

### 阶段1：需求分析
1. **学习项目内容**：
   - 阅读项目文档、代码结构
   - 理解业务需求和现有功能

2. **需求拆分**：
   - 将大需求拆分为原子性需求
   - 每个原子性需求对应一个功能点

3. **任务拆分**：
   - 在原子性需求下拆分任务
   - 每个任务对应需要开发的函数、模块、功能

4. **创建需求和任务**：
   - 在YesDev中创建需求和任务
   - 在本地创建需求文档
   - 确保两端信息一致

### 阶段2：代码开发
1. **开始开发前**：
   - 确认需求和任务已在YesDev中创建
   - 确认本地需求文档已创建

2. **开发过程中**：
   - 如发现问题，在YesDev中创建问题任务
   - 记录开发进度和遇到的问题

3. **完成开发后**：
   - 更新YesDev任务状态为"已完成"
   - 更新本地需求文档
   - 创建变更日志

### 阶段3：测试
1. **建立测试任务**：
   - 在YesDev中创建测试任务（任务类型：4）
   - 关联到相关需求

2. **执行测试**：
   - 进行功能测试、集成测试、性能测试等
   - 记录测试结果

3. **问题处理**：
   - 如发现问题，按问题类型在YesDev中创建任务
   - 修复问题后，更新问题任务状态

## 自动化操作规范

### AI助手应自动执行的操作

#### 1. 需求创建时
- ✅ 调用 `mcp_yesdev-mcp-server_create_need` 创建YesDev需求
- ✅ 在 `docs/200-需求管理/` 创建本地需求文档
- ✅ 记录需求创建时间（默认：2025-12-15 14:00）
- ✅ 确保两端信息一致

#### 2. 任务创建时
- ✅ 调用 `mcp_yesdev-mcp-server_create_task` 创建YesDev任务
- ✅ 关联到对应需求
- ✅ 在本地需求文档中记录任务信息

#### 3. 任务完成时
- ✅ 调用 `mcp_yesdev-mcp-server_update_task` 更新任务状态为"已完成"
- ✅ 记录实际完成时间（`real_task_time`）
- ✅ 更新本地需求文档中的任务状态
- ✅ 检查需求是否全部完成，如完成则更新需求状态

#### 4. 发现问题时
- ✅ 根据问题类型（bug/优化/故障）创建YesDev任务
- ✅ 使用合适的任务标题格式（`[BUG]`、`[优化]`、`[故障]`）
- ✅ 在变更日志中记录问题创建信息

#### 5. 需求更新时
- ✅ 同步更新YesDev需求和本地文档
- ✅ 确保两端信息保持一致

## 时间记录规范

### 需求创建时间
- **默认时间**：2025-12-15 14:00
- **自定义时间**：如用户指定，使用指定时间
- **记录位置**：
  - YesDev：需求创建时间字段
  - 本地文档：需求文档中的"创建时间"字段

### 任务完成时间
- **记录时机**：任务状态更新为"已完成"时
- **记录方式**：使用 `real_task_time` 字段记录实际工时
- **记录位置**：
  - YesDev：任务的实际完成时间字段
  - 本地文档：需求文档中的任务完成时间

## 检查清单

### 需求创建检查
- [ ] YesDev需求已创建
- [ ] 本地需求文档已创建
- [ ] 需求创建时间已记录
- [ ] 两端信息一致

### 任务创建检查
- [ ] YesDev任务已创建
- [ ] 任务已关联到需求
- [ ] 本地需求文档中已记录任务
- [ ] 任务类型和状态正确

### 任务完成检查
- [ ] YesDev任务状态已更新为"已完成"
- [ ] 任务完成时间已记录
- [ ] 本地需求文档中任务状态已更新
- [ ] 需求完成情况已检查

### 问题创建检查
- [ ] 问题类型正确（bug/优化/故障）
- [ ] 任务标题格式正确（`[BUG]`、`[优化]`、`[故障]`）
- [ ] 问题描述详细完整
- [ ] 问题已关联到相关需求（如适用）

## 相关规范

- [文档管理规范](文档管理规范.mdc) - 本地文档管理详细规范
- [任务管理与功能点完成流程](任务管理与功能点完成流程.mdc)
- [EP 项目开发规范](ep.mdc)
- [Git提交规范](Git提交规范.mdc)
